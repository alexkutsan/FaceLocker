#FaceRecognizer (temporary project name)

This programm should detect owner face and lock workstation in case if owner face is not found

## Dependencies
 - opencv
 - log4cplus
 - cmake
 - make
 - g++ or clang with c++11 support

